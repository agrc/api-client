export function Chrome({ children }) {
  return (
    <section className="mx-auto mb-20 mt-28 max-w-prose px-8 text-2xl transition-all duration-150 ease-linear">
      {children}
    </section>
  );
}
