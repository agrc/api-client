export function Chrome({ children }: { children: ReactNode }) {
  return (
    <section className="mx-auto mt-28 mb-20 max-w-prose px-8 text-2xl transition-all duration-150 ease-linear">
      {children}
    </section>
  );
}
