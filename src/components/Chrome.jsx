export default function Chrome({ children }) {
  return (
    <section className="px-8 mx-auto mb-20 text-2xl transition-all duration-150 ease-linear mt-28 max-w-prose">
      {children}
    </section>
  );
}
