export default function Chrome({ children }) {
  return (
    <section className="px-8 mx-auto my-32 text-2xl transition-all duration-150 ease-linear max-w-prose">
      {children}
    </section>
  );
}
